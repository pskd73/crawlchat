{
  "name": "crawl-chat-mcp",
  "version": "0.1.4",
  "description": "A Model Context Protocol server for crawl-chat",
  "type": "module",
  "bin": {
    "crawl-chat-mcp": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js"
  },
  "devDependencies": {
    "@types/command-line-args": "^5.2.3",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "command-line-args": "^6.0.1",
    "zod": "^3.24.2"
  }
}
