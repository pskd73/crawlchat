import { makeMeta } from "~/meta";

export function meta() {
  return makeMeta({
    title: "Embed Demo - CrawlChat",
  });
}

export default function EmbedDemoPage() {
  return (
    <div>
      Embed Demo
    </div>
  );
}
